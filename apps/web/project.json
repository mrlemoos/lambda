{
  "name": "@lambda/web",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/web/src",
  "projectType": "application",
  "tags": [],
  "targets": {
    "dev": {
      "dependsOn": ["@lambda/desktop:generate-icons"],
      "continuous": true,
      "command": "next dev --port 4300",
      "options": {
        "cwd": "apps/web"
      }
    },
    "build": {
      "dependsOn": ["@lambda/desktop:generate-icons", "^build"]
    }
  }
}
