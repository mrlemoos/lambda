# Neon Postgres (Vercel Marketplace resource `lambda`).
# Do not commit real values. Connect with `vercel integration add neon`.
DATABASE_URL=
POSTGRES_URL=

# better-auth (ADR 0006 / 0014)
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:4300
