{
  "name": "@lambda/auth",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/auth/src",
  "projectType": "library",
  "tags": ["type:auth", "pkg:auth"],
  "// targets": "to see all targets run: nx show project @lambda/auth --web",
  "targets": {
    "push-identity": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node --env-file=../../apps/web/.env.local ./node_modules/drizzle-kit/bin.cjs push --config drizzle.config.ts",
        "cwd": "packages/auth"
      }
    }
  }
}
